如果有两个方法的方法名相同，但参数不一致，哪么可以说一个方法是另一个方法的重载。 具体说明如下：

方法名相同
方法的参数类型，参数个不一样
方法的返回类型可以不相同
方法的修饰符可以不相同
main 方法也可以被重载


构造方法不能用户显式调用，只能用new创建对象时自动调用。

  Float a = new Float(5.4);    //正确
  a.Float(5.4);          //错误















