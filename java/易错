如果不想在当前方法内处理异常，可以使用throw语句将异常抛出到调用方法中。

为了明确指出一个方法不捕获某异常，而是让调用该方法的其他方法捕获该异常类的异常，可以在声明方法时，使用throws关键字来抛出该类异常。

String类是final类不可以有子类
String类在java.lang包中

JFrame的默认布局管理器是                      。

（5.0分）
我的答案：

第一空： 
BorddeLayout


JPanel的默认布局管理器是                          。

（5.0分）
我的答案：

第一空： 
FlowLayout



所有事件类的父类是                  。

第一空： 
KeyEvent








































